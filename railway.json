{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && corepack prepare pnpm@latest --activate && pnpm install --frozen-lockfile && pnpm --filter server build"
  },
  "deploy": {
    "startCommand": "pnpm --filter server start",
    "healthcheckPath": "/"
  }
}